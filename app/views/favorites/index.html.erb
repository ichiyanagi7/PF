<div class="container">
  <div class="row">
    <h3>お気に入り一覧(全<%= @mysteries.count %>件)</h3>
  </div>

    <% if @mysteries.present? %>
      <%= render "layouts/mystery",mysteries: @mysteries %>
    <% else %>
    <div class="row">
      <h5 class="mt-3">お気に入りはまだありません。</h5>
    </div>
    <% end %>

</div>